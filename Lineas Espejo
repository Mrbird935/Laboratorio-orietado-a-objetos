int c = 10;

void setup() {

  size(800, 600);
  background(222, 99, 22);
}

void draw() {

  for (int i=0; i < c; i++) {

    float Ancho = width/c;
    float x = i*Ancho;
    line(x, 0, width/2, height/2);
    line(x, height, width/2, height/2);
  }
}
