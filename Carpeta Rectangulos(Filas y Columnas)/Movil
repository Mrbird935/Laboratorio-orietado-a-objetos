class Movil {
  int fila;    // Variable para almacenar la fila
  int columna; // Variable para almacenar la columna
  
  // Constructor ultra-simple: solo recibe los datos mínimos y los guarda
  Movil(int f, int c) {
    fila = f;
    columna = c;
  }
}
